[defaults]
inventory           = inventory/hosts.yml
remote_user         = ubuntu
private_key_file    = ~/.ssh/pem-dir.pem
host_key_checking   = False
role_path          = ./roles
stdout_callback     = yaml
callbacks_enabled   = timer, profile_tasks

retry_files_enabled = True
retry_files_save_path = .retries/
